---
'@umpire/async': minor
'@umpire/core': minor
---

Add `@umpire/async` — async-aware superset of core with async rule predicates, async validators, built-in cancellation (auto-cancel + AbortSignal), and an `onAbort` hook. Core gains a non-public `@umpire/core/internal` subpath for first-party helper access.
