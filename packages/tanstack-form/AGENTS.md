# @umpire/tanstack-form

Type source: @tanstack/form-core (renderer-neutral, v1.29.1). No React/Solid/Vue imports in the root export.

- Framework-neutral helpers export from the root entry (`.`) — validators, adapter, form options, read-backed listeners.
- React helpers export from `@umpire/tanstack-form/react` — `useUmpireForm`, `UmpireFormSubscribe`, `createUmpireFormComponents`.
- Solid helpers export from `@umpire/tanstack-form/solid` — `createUmpireForm`, `UmpireFormSubscribe`, `createUmpireFormComponents`.
- Vue helpers export from `@umpire/tanstack-form/vue` — `useUmpireForm`, `UmpireFormSubscribe`.

Name collision check (2026-05-02): no existing Umpire export or public API conflict found for any of the planned exports.
